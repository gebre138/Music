export interface Track {
  sound_id: string;       
  title: string;
  category: string;
  community: string;
  region: string;
  context: string;
  performer: string;
  description: string;
  sound_track_url?: string; 
  album_file_url?: string;  
}
